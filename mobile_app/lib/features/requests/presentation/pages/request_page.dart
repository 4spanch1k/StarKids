import '../../../../core/widgets/section_placeholder.dart';

class RequestPage extends SectionPlaceholder {
  const RequestPage({super.key})
      : super(
          title: 'Request forms',
          description:
              'Contact, callback, group visit, and birthday requests should '
              'share clear schemas and submission states.',
        );
}


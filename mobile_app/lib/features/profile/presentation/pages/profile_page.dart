import '../../../../core/widgets/section_placeholder.dart';

class ProfilePage extends SectionPlaceholder {
  const ProfilePage({super.key})
      : super(
          title: 'Profile',
          description:
              'Profile stays intentionally lean in MVP: customer basics, '
              'preferred branch, and request history later.',
        );
}

